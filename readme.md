This project simulates a notification system implementation following maintenance guidelines.
Created for Week 4 of CSC6301.

An installation of Java is required to run the program.

To run, cd into the directory that contains Main.java and then execute:
java Main.java

javadoc [class].java -d docs/[class] was executed for each class to generate javadocs.

